name: Main

on:
  workflow_dispatch:
    inputs:
      text:
        description: 'Test.'
        default: 'Hello'
        required: false
        type: string
  release:
    types: [published]

jobs:
  main:
    runs-on: ubuntu-latest
    uses: erictuvesson/github-actions-test/.github/workflows/other-3.yml@main
    with:
      text: "Hello from Main #3"

  other-3:
    runs-on: ubuntu-latest
    uses: erictuvesson/github-actions-test/.github/workflows/other-1.yml@main
    needs: [main]
    with:
      text: "Hello from Main #1"
